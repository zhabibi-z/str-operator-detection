# Generated artifacts — regenerated by running the notebook
artifacts/name_embeddings_minilm.npy
artifacts/*.png

# The scored audit queue is deliberately NOT published. It attaches new risk scores to
# ~19k identifiable real listings under an "enforcement" heading, and §9.4 of the
# notebook is explicit that these scores are triage signals, not findings about anyone.
# Publishing the file invites exactly the out-of-context reading the notebook warns
# against. Run the notebook to regenerate it locally.
artifacts/enforcement_audit_queue.csv

# Python
__pycache__/
*.py[cod]
.ipynb_checkpoints/
.venv/
venv/
env/

# OS / editor
.DS_Store
.vscode/
.idea/
